# 2024 春午後

## 問題1

Gはヘルスケア。利用者がデータ管理。健康リスt九の判定や食事メニューのアドバイス

サービスYを計画→Sシステム

スマホアプリからアクセスする

利用者が入力データを蓄積→機会g句集→ステー辰→優勝利用者→ライブラリで構築

スマホからもアクセスする。Sシステムを用意。

サービスK：APIを受信

サービスL：イベント型駆動外部サービス

サービスM：マネージドのDB

サービスK：がサービスLを呼び出す

ID＋PWで検証する。利用者APIは情報を取得・更新する。

共通モジュールP

JWTはヘッダ、ペイロード、署名の３種類→base64urlでエンコーダ

ライブラリQ：JWTを設定→JWTの検証。Bearerスキーム。Sシステムに送信する。

JWTの署名を検証→レスポンスを返す。ライブラリQに渡す。ペイロードの有効期間まで。ベンダに脆弱性診断を依頼。

ust1で認証成功→JWTのデコード

P呼び出し処理に追加

midに利用者情報を追加→IDと紐づく

セキュリティ強化→ろぐの取得方法を検討

ログ処理モジュールを実装するには時間がかかる→SNを導入

WAFルール→ライブラリHは脆弱性V

ライブラリHの利用。

## 問題1

G社はヘルスケアサービス新興企業。

データ管理、健康リスクのアドバイス：サービスY

Sisuteu を構築→スマホアプリからアクセスする。

F社に委託。システム構築を。クラウドサービスプロバイダを構築する方針。

サービスＹのシステム構成。E社が提供するサービス。

A-APIリクエストはKが受ける。

リクエスト内容に基づき、サービスLを呼び出す。

事前に定義された処理でサービスK。レスポンスを返す。

G社スマホアプリからS-APIを呼び出された動作。サービスKが一元的にうける。→Lを呼び出す→Kを返し、レスポンスを返す

利用者IDとPWを検証する。IDとPWを事前登録と検証。

事前に登録されたものと一致した場合、ランダムに生成される数字４桁の文字列X。メアドに送信する。G社アプリから受信した利用者IDと４桁数字を検証する。

→JWTを発行してレスポンスを返す。

共通モジュールPと呼び出し処理はLにおいて定義されている。

JWTの管理にライブラリQwo利用するbearerスキームを設定してSしうsてむに送信する。

JWTを指定のALGで検証。課金サービスを利用する。診断を依頼→診断レポートを示す

Sシステムの構築→脆弱性診断

JWT改ざんによるなりすまし。アクセスコントロール、paid優勝利用者に改変出来る

midに利用者IDを利用してデータを取得する

midに利用者IDを指定で他利用者を取得。APIで指定できる。

２要素の突破。

JWTデコード。ヘッダ＋ペイロード。

ヘッダとペイロードを改ざん→JWT

脆弱性V。攻撃対象サーバ。LDAPとHTTP

LDAPリクエストを作成→Vを悪用する

## 問題1

G社はヘルスケアサービス新興企業。

データ管理、健康リスクのアドバイス：サービスY

Sisuteu を構築→スマホアプリからアクセスする。

F社に委託。システム構築を。クラウドサービスプロバイダを構築する方針。

サービスＹのシステム構成。E社が提供するサービス。

A-APIリクエストはKが受ける。

リクエスト内容に基づき、サービスLを呼び出す。

事前に定義された処理でサービスK。レスポンスを返す。

G社スマホアプリからS-APIを呼び出された動作。サービスKが一元的にうける。→Lを呼び出す→Kを返し、レスポンスを返す

利用者IDとPWを検証する。IDとPWを事前登録と検証。

事前に登録されたものと一致した場合、ランダムに生成される数字４桁の文字列X。メアドに送信する。G社アプリから受信した利用者IDと４桁数字を検証する。

→JWTを発行してレスポンスを返す。

共通モジュールPと呼び出し処理はLにおいて定義されている。

JWTの管理にライブラリQwo利用するbearerスキームを設定してSしうsてむに送信する。

JWTを指定のALGで検証。課金サービスを利用する。診断を依頼→診断レポートを示す

Sシステムの構築→脆弱性診断

JWT改ざんによるなりすまし。アクセスコントロール、paid優勝利用者に改変出来る

midに利用者IDを利用してデータを取得する

midに利用者IDを指定で他利用者を取得。APIで指定できる。

２要素の突破。

JWTデコード。ヘッダ＋ペイロード。

ヘッダとペイロードを改ざん→JWT

脆弱性V。攻撃対象サーバ。LDAPとHTTP

LDAPリクエストを作成→Vを悪用する

## 問題2

H-PC、VPNクライアントソフト

リモート接続用PC→VPN-GW→VPN-H

VPNりおーとをしてうｒ。ＦＷ→ＮＡＴ→ＩＰＳ→ＷＡＦ

ダイアログで入力、多要素認証

DDoS攻撃による被害

社内ネットワークに親友→脆弱性を調べるポートスキャンの応答を返さない方法を検討。

inetよりVPN-Hへのアクセスは多要素認証

IPS機能。VPN-GWの認証情報を推測。

V1: 多要素認証の利用が多くなってきた。

設定P:所定のポートへの接続を許可する。

SPAパケットを検証する。最後尾フィールドハッシュ値が格納されてる。

H社は製造業。HPPTSでアクセスしログイン。H社とのやり取りのアクセス。

公開WEBサーバでは要望の受付を実施。HPCとは別にリモートPCを貸与→RMT

ファイアウォール；STインスペクション。通信許可と制御。

VPN通信機能。ダイアル後に利用者IDとPWを入力。SMSで入力される。サイバー攻撃が２件立て続け。

VPN-GWが攻撃を受けた事案。DDoS攻撃による被害が発生。

VPN-GWの認証情報を推測して、ネットワークに侵入認証強化を検討することにした。

対策V-1

多要素認証の利用がおおくなってきた→多要素認証を狙う攻撃麗。

対策V-3

VPN-Hにdeny-allを設定して、所定のポートへ接続を許可する設定。

## 問題3

自社のホームページやECサイトのWEBサイトに診断を委託。リリース後も継続している。手動による高度な分析も行う

サイトXとサイトYをクラウドW上に構築している。YはXの規格を。DBで構成する

CMSが存在。WEBアプリのコンテンツ。サイトP

D社は共同研究→サイトPで情報交換。サイトYはサイトPの情報を表示する。クラウドW利用の際には、D社のクレデンシャルでログイン出来る。

IMDSは特定の情報を返す。メタデータストレージサービス。CMSはPOSTはOK。

サイトXはりおゆしゃ会うあkンとがある。ログインセッションはcookieにて。Secure属性がセットされる。サイトXの機能。

構築を「行った→アプリリリース。診断。

name=にスクリプト。脆弱性はサイトX。利用者アカウントとD社管理者用アカウント。

CSRFを検出。

認可制御の不備。αとβを用いて注文履歴を閲覧した。

## review問題3

D社は小売業。HPやECサイトのwebサイトではwebアプリの診断を委託。サイトXと規格サイトY→Wクラウド。

CMSを使って構築。サイトPにより情報交換している。ストレージWを利用している。

サイトXには利用者アカウントがある。サイトXのログインセッション管理はcookieで。secure属性。

secure属性→HTTPSの時のみ、cookieが送付される。

サイトXの機能一覧。ログイン。商品コードが付与されている。サイト管理機能。XSS、CSRF、認可不備。

認可不備。

## 問題4

webアプリケーション。授業イン５００の加工会社。業務システムを利用している。サーバに構築されている。

web受注システム。

商品検索、在庫照会など。アクセス方式はブラウザ。→構築する。

重要情報取り扱い→個人顧客からの問合せい対応。B社が開発。OSアカウント一覧の策定。所属グループ。

システム開発者：プログラムの保守を行う。

システム管理責任者：OSやMWの脆弱性修正など。

業務システムと受注しうsてむはCSVでデータ連携。バッチで受信→CSVファイルを保存する。

注文テーブルの連携済みフラグが0の注文データをCSVでは平文。バッチファイルに重要情報が閲覧できる。

業務システムとweb受注システムがデータ連携を行う。CSVファイルを作成→バッチに送信。CSVファイルを保存する。

ユーザー用得機能のレビュー。UserDataクラスの実現。

## review 問題4

A社は加工食品。業務システムを利用している。受注システムを構築することになった。

アクセス方式、想定ユーザー、基盤はIaaS上に構築する。

APサーバ、バッチサーバ、ログサーバでLinuxを利用する。

テキスト出力Apサーバへ。

PWはCrystrecのものを使う。

1hごとに業務システムのバッチサーバと受注システムにおいてCSVファイルを作成→DBに対して更新処理を実行うる。

ユーザーはUserDataプログラムの私用。コードについて。

# 2024 春午前

#### X.509の基本概念

X.509とは、公開鍵基盤（PKI: Public Key Infrastructure）において使用されるデジタル証明書の標準フォーマットです。

デジタル証明書の標準フォーマット

バージョン情報、シリアル番号、発行者、有効期限

TLS/SSLを活用したウェブサイトの暗号化通信において不可欠

ISMAPは政府が求めるセキュリティ要件を満たしているクラウドサービスを評価することでセキュリティ水準の確保を図り、クラウドサービスの円滑な導入を助ける制度。

CMMI:組織のプロセス改善の能力・成熟度を評価するフレームワーク

SIM3：CSIRTを構築、運用するための指針

# 2024 秋午後

## 問題1

ソフトウェア開発会社。社内は情シス。運用インシデント。EのSaaSを利用する。L社ドメインを管理する。

ドメインユーザーが割り当てられる。Qプログラムをホストにインストール。L社内ホスト。

URLフィルタを適用→再暗号化

ドメインごとのアクセスは許可or拒否。

マルウェア対策サービス。定義ファイル、適用状況の確認が可能。

仮想PC。RDPで接続可能。

インシデント対応にログの調査にyrmsふぉyyr津。→Fツール。ログの収集を実施。仮想PCで実行。アラームの受信。

PC-Aにアクセスしようとしてアクセス拒否。

PC-Aの将校データとアプリの解析。

q.ps1をメモリ展開→実行する。DLしたもの。

マルウェアを停止する。アップロードする。コネクトバックする。


## 問題2

A社工作機械。WEBサイト。IR＋関連情報。メールを用いて、見積の送受信。

A社のサイトとドメインとして利用。Wサービス。

情報システムはメール詐称。z-sha.co.jp

Z社変更。Sサービス。SPFとDKIM。送信するメールのドメイン。DMARC＋SPF＋DKIM署名。コンテンツをZ社に変更。A社はリダイレクト

DMARCポリシーはアクション要求しない。

セレクタ._dmainkey.ドメイン

A社ドメイン。海谷宇。WEB悪用。コンテンツ細工して、影響を及ぼす。連絡先の変更で悪用する

Uサービス以降

Mail=step2

一斉配信：Tサービスを利用する。

メーリングリストの利用。登録メンバより送信できる。Yサービスを利用する。

## 問題3

J社ECサイトを運営する。webアプリP、サーバで構成される。

アプリサーバログには陸枝うとラインが記録される。クレジットカードに対応した決済。

ECMAScriptwoを利用したスクリプトトークン決済。

カート法品を表示する。廃掃法選択。支払い手続き。

不審なアクセス。


## 問題4

Bsha セキュリティ診断会社。Mサイト。

個人情報を入力する。企業とマッチングを行う。

MサイトはWebアプリとインターフェースの開発が完了。

本番サイトと検証サイトがある。診断対象はMサイト。WEBアプリが導入。同一の構成。

B社からもアクセスできるように設定。B社は診断用PCからアクセス。

プラットフォームの脆弱性は診断ツール。APIkey

設問の画面遷移。Mサイトから変更前のメールアドレスを変更した画面遷移。

セキュリティ診断→Mサイトの脆弱性。ログインしてない状態だとエラー

メールヘッダーインジェクション

総合評価→気づかれることなく悪用される。

WebAPIの利用権限をもつ求人企業のみ。

より多くの求人情報を取得することが出来る。

# 2024 秋午前

STI/TAXII: サイバー攻撃活動の脅威情報を記述するための標準規格。

DTLS

TLSと同様に暗号化、認証、データの整合性を提供しますが、コネクションレスな特性を持つため、リアルタイムアプリケーションやVoIP、オンラインゲームなど、遅延が重要な環境で特に有効です。

主にUDP(User Datagram Protocol)を利用する通信において、データの暗号化、認証、整合性の保証を提供します。

DTLSは、リアルタイム性が要求されるアプリケーションや、データ損失が許容される環境でのセキュリティ確保に適しています。

### 主な特徴

* **セキュリティの確保** : データの暗号化、相互認証、データ整合性の保証を提供。
* **UDP対応** : TCPに対する代替として、信頼性の低いプロトコル上でもセキュアな通信を実現。
* **再送制御** : パケットの順序や再送に対応し、セキュリティの維持を図る。
