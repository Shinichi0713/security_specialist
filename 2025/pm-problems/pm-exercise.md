
## 18

1. ネットワークより一時的に切り離した対応
2. 圧縮ファイルを展開すると、展開したファイルに対してリアルタイムスキャンが実施されるため
3. ファイル転送時の操作手順は置き換えて読む：ファイルをダウンロードする必要があるため。
4. パスワードレス認証方式ではAUthnを利用：オリジンbとオリジンsの一致性を確認しているから。
5. 製品Rはファームアップ：サーバ証明書を検証し、通信相手がWサーバであることを確認する実装。
6. ネットのDBには会員情報が登録されている。マルウェアの目的は採掘プログラムをDL。マルウェアXに採掘演算結果以外の情報を外部に送出する機能はなく、マルウェアX以外による遠隔コマンド実行やSSHへの接続がなかったため。
7. DシステムからSaaSを前提として検討。FIDOが良い：Dシステムへのアクセスを、ファイル受け渡しPCからのアクセスに限定できるため。

## 19

1. a:DVD-R経由で b:マルウェアファイルの更新 c:マルウェア対策ソフトの画面の操作
2. 社外の通報窓口を設置する
3. 既知の脆弱性がないかを確認する
4. デバッグログにはIDごとのセッション情報：デバッグログに認証情報を出力しないこと。
5. 最初の判定に加えて、影響の大きさ又は影響の広がりについての事実が見つかるたびに再判定を行う。
6. h:生体認証、i:Kサービス、j:アカウントを削除、k:アカウントを無効に
7. 2-b：2-aと同じメッセージを表示する
8. 複数の脆弱性が同時に悪用される可能性の観点。
9. 管理者がPソフトを一般利用者権限では変更できないように設定する。

## 20

1. サイトRのアカウントとそれ以外：ポイント交換、不正購入
2. HTTPで接続が開始されたため
3. webメールの偽氏とに誘導。フィッシングの手口。OTPの入力を要求し、OTPを認証サーバーに中継する処理
4. ログ。MWMに感染、感染後に分離された場合
5. S認証モジュールに利用者IDとパスワードを登録していないS会員
6. 連携サーバは認証なし。7/14
7. ワームV。TCPスキャン。同一IPアドレスへのスキャン回数は少ないため
8. WEBサーバーNを用いて決済。エラーメッセージが異なるという挙動。
9. なりすましによるアクセスの場合、操作した人物とログに記載された利用者IDの利用者が異なるため。
10. 登録されていないメールアドレスに対してメールを送信できる。
11. STは認証サーバに送信されないため。
12. アクセスがあったとき、協働出品担当メンバはB社にいて、KさんのDPCを使用できないため。
13. 実機環境を用いる
14. C&CサーバへのHTTP通信成功のログ。DNS通信の成功ログ。
15. ｋ：ファイル圧縮、l：ファイル名、ｍ：ファイルサイズ

## 21

1. ゼロデイ攻撃
2. IPアドレスより判別
3. 登録した実行ファイルがバージョンアップされた場合
4. 登録した実行ファイルのマクロとして実行されるマルウェア
5. ネットワークトラフィックと比較して、異常を検知する。
6. スマートフォンが盗まれた場合
7. A社公開webサーバの名前解決が出来なくなる。
8. 推測されやすいPINコードが設定される
9. 飲食業のスマホ。ポイントアプリの仕組みを利用してバーコン０－度を表示する。他者の会員番号を窃取してバーコード化し、決済する。バーコードの生成が会員番号のみであること。
10. 残存するマルウェアが存在するDPCがないか確認するため。
11. 0～８時に実施する。
12. 設計秘密はWソフトで暗号化。パスワード変更、ローカルPCにコピー

## 22

1. MACの生成に共通鍵を利用する。管理するゲームサーバ上で動作する全プログラム
2. ゲームサーバに認証サーバと同じ共通鍵を保存する。
3. Tサービスの障害児にSサービスを利用できなくなる。

## 23

1. マルウェアB待機、横展開、遠隔。待機か、横展開のみを実行している場合。
2. progはない。progをダウンロードして実行する命令。
3. ルータAのファイアウォール。WAN側からLAN側への通信の設定が変更される。
4. 標準ソフトウェア以外のソフトウェアは脆弱性管理がされないという不具合。
5. FWからみた保守用中継サーバ。ｂ：保守PC-A、c：インターネット
6. ネット→中継SVは拒否。6/14の7:00～9:30まで。
7. CCをインストールしたPCを協力者充てに輸送
8. 社内PC、内部メールサーバ、SMTP。
9. 複数IPアドレスより、不正アクセスを続けた場合
10. C&Cサーバとの通信時にDNSへの問い合わせを実行しない場合があるため。
11. 同一利用者IDによるログイン失敗
12. メールサーbsにSMTP転送。ブラウザによってメール送受信。送信者メールアドレスの詐称防止機能が使えなくなる。インターネットのメール送信が許可されていない従業員がメール送信できる問題。

## 24

1. 2要素認証PW方式による認証に追加可能なもの。クライアント証明書によるデバイス認証
2. C社情報システムでクライアント認証。サービスQ。クライアント証明書を検証する。
3. クライアント証明書によるデバイス認証を行う仕組み
4. 業務での個人利用NG。IDああSQについて調査。秘密鍵を書き出し出来ないようにする。

## 25

1. OCSP
2. DKIM利用時メールサーバ。DNSSV。メール本文、メールヘッダへの改ざんの有無。
3. コードサイニング
4. クライアント証明書の提示が必要な外部WEBサーバにアクセスする
5. FWの製造元によって安全性が確認されてないCAが発行したサーバー証明書を使用した外部webサーバーにアクセスする
6. FQDN
7. 署名用電子証明書の有効性
8. HAMCαとHMACβの値が一致することを確認する
9. メール発行として登録。ディジタル署名、検証、MLの登録メンバ、ML

## 26

1. DNSキャッシュぽいずイング
2. 古リゾルバ。外部キャッシュポイズニング。A
3. 外部DNSサーバ、CNAME
4. メールサービスP、攻撃者が用意したWEBサーバ
5. 決済時に利用者のスマホが確実に通信できる。無線ルータは同一機種。管理者機能のログイン画面にアクセスできる。い。攻撃者のDNSサーバのIPアドレス
6. DNSSEC

## 27

1. プロキシサーバーのログよりIPnのサイトにアクセスした機器がほかに存在するか
2. サイトMのログの元。プロキシサーバのログよりサイトMがアクセス先となったエントリを抽出し、このエントリよりPC-AのIPアドレスを取得した。
3. FW1、プロキシサーバ
4. FW1、サイトZとの通信履歴を確認する

## 28

1. FSVの３台。hostファイルにはプロキシなど。マルウェアに感染したGPC。アクセス元を制限しないで、アクセス先がC&Cサーバであるアクセス。
2. ファイル名、ファイルサイズ
3. h: ファイルサイズ、i: アップロードされたファイルサイズ、j: アクセス先のURL
4. Rログをマルウェアのハッシュ値で検索する
5. チャットRの中でされている会話の内容
6. イベントログの削除を示すログ
7. w1.x1.y1.z1との通信履歴
8. DMZ上のプロキシサーバとの間にFWが介在。プロキシサーバログに記録されたファイルサイズ
9. PC-A送信元。化学コンソーシアム。
10. IPリストに登録されたIPアドレス
11. 連携端末以外のIPアドレスのIPアドレスを送信元とする通信記録

## 29

1. 特定のドメインに対する多数のDNSクエリの発生
2. 権威DNSサーバ、外部DNSサーバ、再帰的クエリ
3. C&Cサーバへのコマンド要求または応答、C&Cサーバからのコマンド受信

## 30

1. 正確に処理できる、作業が早い
2. 攻撃
3. 管理者に通知
4. UTMはファイアウォールとIDS。システム管理者に通知。UTMのIDS機能によって攻撃が検知でき、システム管理者に連絡されるため
5. 設計秘密ファイルを扱う。IRM製品。アクセス権限の付与。アカウントはファイルを開くことが出来る。アイ
6. EDRはWoL。PCのARPテーブルから活動に必要な情報を読み取って保持。エージェントによって、夜間にARPを検知したら、当該PCをネットワークより隔離する。
7. マルウェアはPCのMFにある電子メール。①メールフォルダ内のファイルが読み込まれた②HTTPでファイルがアップロードされた
8. EDRの製品C。イベント記録。製品Cの出荷時に組み込まれている検知ルール。ファイル操作など。MWβ手段による感染拡大を検知するルール。ファイルが読み込まれた後に１分以内にファイルが上書きされる。
9. 管理するUSBIDのいずれにも一致しないUSBーIDのUSBメモリが装着された

## 31

1. レビュー、第三者
2. 脆弱性を作りこんだ原因を調査して、注意すべきポイントを追加する。
3. 多要素認証の実装をT社がやらなくても良い
4. 半年に１度の休止期間
5. 半年に１度改良リリースの期間を長くする。いたーレーション１階分を改良リリースとして用いる
6. Admin+数値とした文字列

## 32

1. 動画サービスを提供するX社のサーバが利用する。FQDNが発行される。FQDNとCDNを紐づける。Host
2. WF-Kに脆弱性Kが存在する。HTTPリクエストを送信すると。攻撃に用いられる文字列がPOSTデータに含まれている場合
3. UnixベースのNAS。POSTメソッドで送信したボディがアクセスログに残っていなかったから。
4. Libuxのファームを持つ。脆弱性Bが悪用される。攻撃リクエストをPOSTメソッドで送信させるスクリプトを含むページを表示させる仕組み。
5. レジストリサーバ特有のレスポンスヘッダ
6. TLSの接続先サーバー名

## 33

1. B社のwebサイト。サイトZ。returnURLには登録されていない駅名のURL。V氏が用意したサイト
2. codeの値を限定商品の値に書き換える
3. 診断用の利用者ID
4. RポータルXSS。脆弱性の有無のよりレスポンスに違いが出ないため
5. スクリプトを分析して、フラグメント識別子の変換による挙動の違いを確認する
6. yuuryouのみが購入できる限定商品を購入できる。k: 権限が異なる複数の  l:許可されてる操作の違い
7. topic以降の値をhttps://db-y.b-sha.co.jpに変更した。
8. クエリ文字列のidに未参加のプロジェクトIDを指定する
9. 製品Rはlinuxのファームウェアを持つ。シェルが実行するコマンドをパラメータで不正に指定できて

## 34

1. Same-Origin
2. URLと同じオリジンであるスクリプトファイル
3. https://site-a.m-sha.co.jp
4. 売れ筋商品情報配信の申込ページのオリジン
5. h: Originヘッダーフィールドの値、 i: 許可するオリジンのリスト、j:一致
6. c:ディレクトリ、d:クロスサイト、e:HTTP、f:ジャッキング
7. 改行コード
8. Rポータルはセッション管理をCookieで実現。Rポータルが利用しているスクリプトがcookieを読み出している場合
9. スクリプトZがサイトBから情報を取得する。サイトBへのログイン。
10. %lt;, %gt;
11. returnURLの値を固定とする
12. http://または、https://で始まるURLのみを出力する
13. HTMLファイル中に記載されたスクリプト、スクリプトを別ファイルとして同一オリジンに保存してHTMLから呼び出す。
14. CSRF対策用トークンの発行、HTMLへの埋め込み、紐づけ、検証する処理。
15. プロジェクトを利用者が指定できなくなるため
16. 推測困難である

## 35

1. プレースホルダー
2. ヒープ
3. shellコードがDEｐで実行禁止にされているスタック領域に存在するため
4. ASLR
5. m_note = NULL;
6. strcpy
7. canary
8. コンパイルにSSPを適用することが出来ない環境。メモリ破壊攻撃が防げないこと
9. 共有ライブラリはDEPにより実行禁止にされない領域に配置されているため
10. DisplayNote
11. 785634120a
12. ポインタを使って直接メモリを操作しているため。
13. 名前例外。childChecked=false;→child=true;NamingExceptionを投げる前にchildCheckedをfalseにする。
14. 情報表示機能を持つ。①プロジェクトのみ。プロジェクトID。情報番号＝？ AND プロジェクトID=?

## 36

1. C-PCを貸与。個人所有の機器。個人所有機を多く接続したため、割り当て可能なプライベートIPアドレスが枯渇するという問題が引き起こされた。
2. 稼働させたまま：L2SWにミラーポートを設定し、ポートにLANモニタを設置してDHCP OFFERの数を確認する。停止させる：DHCPによるIPアドレスの配布がないことを確認する。
3. SYN+ACK
4. キャッシュ
5. L2SW1
6. パケットがNSMセンサの対象外のため
7. 起動パケットをほかのセグメントにも転送出来るように変更する
8. IPアドレス、MACアドレス

## 37

1. VLANを用いてPC間の通信を禁止する。
2. 権威DNSサーバがサービス停止となるリスク
3. 攻撃者の操作命令がFA端末に伝えられないこと
4. APに不正接続しても、FAネット内の端末にはアクセスできないため。

## 38

1. g:電波を傍受 h: MACアドレス
2. 無線LANポートのMACアドレスを、総務部のW-APに登録済みのMACアドレスに変更する。
3. ホテルWi-Fiと同じSSIDと事前共有鍵

## 39

1. MAIL FROM
2. 送信側のDNSサーバに設定されたIPアドレスとSMTP接続元のIPアドレスが一致しないため。
3. x1.y1.z1.1
4. z1.y1.z1.4
5. 送信ドメインの利用検討。Envelop-Fromにはサブドメインのメアド。k: a-sub.n-sha.co.jp l: x2.y2.z2.1
6. m: quarantine  n: r
7. メールサーバ
8. N社の取引先と似たメールアドレスより送信ドメイン認証技術を利用してメールを送信する。

## 40

1. T-IDをサービスに登録。T-IDがSサービスに登録されていることを確認する。
2. 偽造
3. LDAP
4. 一度のログインで全システムにアクセスできるという利便性
5. KサービスFUDI。ブラウザから認証サーバへ接続。Kサービスにログイン。認証器からPCのログイン。K差０ビス認証サーバへの接続。お魯迅がKサービスのものと確認。メッセージをKサービスとの間で中継

## 41

## * Sファイル共有サービス。IDPW。多要素認証。linux。NAX背品。製品ではファームウェア。sudoコマンドの設定ファイルより、tarコマンドのオプションを受け付けないようにする。

* LinuxのNAS製品。web操作ではアクセスできないフォルダのファイルが暗号化されているため。
* U社の予約サーバはTソフトをつかっている。runプロセスが稼働している原因。親プロセスがTソフトのプロセスであるため。
* 攻撃の接続元IPアドレスを"/etc/hosts.allow"ファイルに追加する。
* 支援サーバは開発支援ツール。脆弱性制Mを悪用しても一般利用者権限での操作であるが、"/etc/shadow"ファイルの閲覧には管理者権限が必要だから。

## 42

1. 公開鍵認証方式
2. 接続先が保守用中継サーバではない
3. パスワード認証
4. 保守イン以外が不正に秘密鍵を利用できないようにするため
5. 秘密鍵

## 43

1. 耐タンパ性
2. USBをマルウェア対策ソフトでスキャンする。
3. TPMにSKを肘bbする。ハッシュリストを作成。TPMにハッシュ値リストを保存する。
4. ゲームVはストレージと認識されない。SSDに格納する。SSDを取り出しPCと接続する。

## 44

1. ディジタルフォレンジックす。ディスクイメージ。
2. 削除されたファイルの内容。秋セクタの情報からファイルを復元する。
3. メモリ上の情報が失われないようにするため
4. PCのネットワークインターフェースの通信の状態について情報が失われること
5. 一時ディレクトリのログ

## 45

1. c: 同一の暗号ブロック d:平文ブロック e: カウンタ値を暗号化した値
2. c: 60, d: 196
3. IPヘッダのTCPヘッダ部は同一バイト列が連続しやすいこと。
